{
  "schemaVersion": 1,
  "id": "io.github.erikburdett.tokenstats",
  "name": "Token Stats",
  "version": "1.5.0",
  "author": "Erik Burdett",
  "license": "MIT",
  "description": "Local LLM tokens per hour in the bar, with an exact-count graph, history, cloud comparison, and Claude Code / Codex agent tracking with a unified resumable sessions list",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "TokenStats.qml"
  },
  "barWidget": {
    "displayName": "Token Stats",
    "description": "Tokens per hour in the bar, with a graph, history, savings versus a hosted API, and every AI session — local and cloud — in one resumable list",
    "category": "System",
    "aliases": [
      "tokens",
      "llm",
      "llama",
      "stats",
      "agents",
      "claude",
      "codex"
    ],
    "allowMultiple": false,
    "defaultSection": "left",
    "defaults": {
      "barPeriod": "Today",
      "refreshIntervalSec": 10,
      "cloudInputPerMillion": "3.00",
      "cloudOutputPerMillion": "15.00",
      "cloudCachedInputPerMillion": "0.30",
      "systemWatts": 120,
      "pricePerKwh": "0.12",
      "currencySymbol": "$",
      "endpoint": "auto",
      "importOpencode": true,
      "importClaude": true,
      "importCodex": true
    },
    "schema": [
      {
        "key": "barPeriod",
        "type": "enum",
        "label": "Bar shows",
        "description": "Which window the number on the bar covers. The popup can show any of them.",
        "options": [
          "This hour",
          "Today",
          "7 days",
          "30 days",
          "12 months",
          "All recorded"
        ],
        "defaultValue": "Today"
      },
      {
        "key": "refreshIntervalSec",
        "type": "integer",
        "label": "Refresh (seconds)",
        "description": "One loopback request per resident model per refresh. 10s is plenty; counters are cumulative so nothing is missed between polls.",
        "min": 2,
        "max": 120,
        "step": 1,
        "defaultValue": 10
      },
      {
        "key": "cloudInputPerMillion",
        "type": "string",
        "label": "Cloud price per 1M prompt tokens",
        "description": "Set this to the API you would otherwise have used. Shown on the popup so the savings figure is never a hidden assumption.",
        "defaultValue": "3.00"
      },
      {
        "key": "cloudOutputPerMillion",
        "type": "string",
        "label": "Cloud price per 1M generated tokens",
        "defaultValue": "15.00"
      },
      {
        "key": "cloudCachedInputPerMillion",
        "type": "string",
        "label": "Cloud price per 1M cached prompt tokens",
        "description": "Hosted APIs bill prompt tokens served from their cache at a reduced rate rather than free. Most local prompt tokens are cache hits, so this rate, not the full one, dominates the comparison. Default is a tenth of the prompt rate.",
        "defaultValue": "0.30"
      },
      {
        "key": "systemWatts",
        "type": "integer",
        "label": "System draw while generating (W)",
        "description": "Used with the price below to cost the electricity your own hardware spent.",
        "min": 0,
        "max": 2000,
        "step": 5,
        "defaultValue": 120
      },
      {
        "key": "pricePerKwh",
        "type": "string",
        "label": "Electricity price per kWh",
        "defaultValue": "0.12"
      },
      {
        "key": "currencySymbol",
        "type": "string",
        "label": "Currency symbol",
        "defaultValue": "$"
      },
      {
        "key": "endpoint",
        "type": "string",
        "label": "llama.cpp endpoint",
        "description": "Leave as \"auto\" and the plugin finds it: it probes the usual loopback ports until one answers, and works with either llama-swap or a llama-server run directly. Set an explicit http://127.0.0.1:PORT only if yours is elsewhere. Loopback only \u2014 anything else is ignored.",
        "defaultValue": "auto"
      },
      {
        "key": "importOpencode",
        "type": "boolean",
        "label": "Import history from OpenCode",
        "description": "Fills in tokens generated before this widget was installed, or while the shell was not running, using OpenCode's own exact per-reply counts. Read-only, local models only, and never double-counted.",
        "defaultValue": true
      },
      {
        "key": "importClaude",
        "type": "boolean",
        "label": "Track Claude Code",
        "description": "Reads the exact per-message usage Claude Code records under ~/.claude for the Agents view and the unified sessions list. Read-only, and kept apart from the local savings figure.",
        "defaultValue": true
      },
      {
        "key": "importCodex",
        "type": "boolean",
        "label": "Track Codex",
        "description": "Reads the exact per-turn usage Codex records under ~/.codex for the Agents view and the unified sessions list. Read-only, and kept apart from the local savings figure.",
        "defaultValue": true
      }
    ]
  }
}
